Install:
* debian/ubuntu: `apt install libimlib2`
* arch: `pacman -S imlib2`
* fedora: `dnf install imlib2`

Compile the client by running:

```gcc -o pc pixelflut.c -Wall -Wextra -lImlib2```

Run it using the `pc` executable.
